import React from 'react';
import {SafeAreaView, ScrollView} from 'react-native';
import Home from './ui/Home';

export default () => (
  <SafeAreaView>
    <ScrollView>
      <Home />
    </ScrollView>
  </SafeAreaView>
);
